---
description: 代码注释、调用链说明与架构简洁约定
alwaysApply: true
---

# 代码注释、调用链与简洁

## 1. 注释怎么写

不要写空泛、套话、复述文件名/类名的注释。

注释只写读者真正需要的信息，按方法功能二选一或组合：

- **做什么**：这个方法在流程里完成哪一步（一句话）
- **输入 / 输出**：关键参数是什么、返回什么；有副作用时点明（例如改内存状态、抛哪类错）

### 长函数必须分段（硬性）

若函数体大约超过 ~40 行，或包含多个明显阶段（准备 → 循环 → 收尾），**必须**用短注释标出阶段边界，让人扫一眼知道走到哪了。

推荐形式（二选一，全文件统一即可）：

```python
# --- 1) 准备权限 / hooks / 工具表 ---
...
# --- 2) 收 LLM 流：正文 / 思考 / 工具分片 ---
...
# --- 3) 执行工具并写回 messages ---
```

阶段注释写「这一段在主流程里干什么」，不要写废话。

阶段内部仍遵守「不对显而易见代码逐行旁白」；但**阶段切换、非显然分支、循环继续/结束条件**必须能被注释或命名看出来。

禁止：

- 「用于 xxx 的辅助类」「实现某某任务编号」这类和阅读无关的噪音
- 大段复述 OpenSpec / 设计文档原文
- 对显而易见代码逐行旁白
- 一百多行主流程零阶段注释（即使有函数 docstring 也不够）

## 2. 代码内容与架构要简洁

优先写短、直、少层的代码；架构只保留当前任务真正需要的抽象。

- 一个类/方法只做一件事；能合并就不拆，能内联就不抽工具类
- 长流程若阶段清晰，优先「阶段注释 + 抽出已完整的一步」（如「执行本轮工具」），而不是再套一层空架子
- 不为「将来可能」预留接口、工厂、策略、包装层
- 避免平行的重复结构（同类 DTO/Exception/Service 只因命名不同而膨胀）
- 能用现有类型与调用链解决的，不新开包、不新开层次

宁可少几个文件、少一层间接，也不堆「看起来完整」的空架子。

## 3. 写完代码要串起来

每完成一小批相关方法/类后，在回复里用简短调用链说明：

- 列出本批改动涉及的文件，使用完整路径说明每个文件改了什么
- 对关键类点名列出关键方法（包括构造器或启动钩子），说明方法在流程中的职责、关键输入输出，以及重要分支、异常或副作用；不能只写类级概述
- 这些方法分别在主流程的哪一步被调用（或「目前仅被谁调用 / 尚未接到主流程」）
- 从上到下使用“类.方法”说明谁调用谁、数据怎么传（例如 keyId → 私钥 → 解出 AES key）

不要只丢一堆彼此孤立的类/方法列表让读者自己猜关系。
若当前任务只造零件、主流程尚未接线，必须明确写清「尚未被 Controller/Advice 调用」，并指出预计在哪一步接入。
